/**
 * Labeling's colour on the home page, as a chart series slot (`useChartColors`):
 * the New job shortcut and the Labeling rows share it, so labeling reads as one
 * thing across the page.
 */
export const LABELING_SLOT = 6;
